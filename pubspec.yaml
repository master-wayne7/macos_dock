name: macos_dock
description: "A Flutter package for creating a macOS-style dock."
version: 1.0.0
homepage: https://github.com/master-wayne7/macos_dock

environment:
  sdk: ^3.6.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

example: main.dart

flutter:
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
funding:
  - https://www.buymeacoffee.com/ronitrameja
